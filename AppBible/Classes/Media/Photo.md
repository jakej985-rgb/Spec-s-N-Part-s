# Photo

---

## Purpose

Data model representing photo attachments associated with service records, vehicle modifications, or documentation.

---

## Properties

* `photoType` (`PhotoType`): Category enum (`service`, `powertrain`, `exterior`, `interior`, `document`, `other`).
* `path` (`String`): File system or local asset path of the photo.
